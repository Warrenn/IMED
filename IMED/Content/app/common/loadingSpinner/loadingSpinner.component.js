import MediaQueryFactory from '../../common/mediaQuery/mediaQuery.factory';

import controller from './loadingSpinner.controller';
import './loadingSpinner.less';
import template from './loadingSpinner.page.html'

let loadingSpinnerComponent = {
  bindings: {},
    template,
  controller
};

export default loadingSpinnerComponent;
